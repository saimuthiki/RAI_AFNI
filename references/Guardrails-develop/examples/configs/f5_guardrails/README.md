# F5 AI Guardrails Example

This example demonstrates how to integrate with the F5 AI Guardrails API.

To test this configuration you can use the CLI Chat by running the following command from the
`examples/configs/f5_guardrails` directory:

```bash
uv run nemoguardrails chat --config=.
```
