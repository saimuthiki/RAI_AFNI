apiVersion: v1
kind: Service
metadata:
  name: {{ include "thyris-sz.fullname" . }}
  labels:
    {{- include "thyris-sz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "thyris-sz.selectorLabels" . | nindent 4 }}
