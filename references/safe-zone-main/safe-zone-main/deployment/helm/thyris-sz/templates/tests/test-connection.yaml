apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "thyris-sz.fullname" . }}-test-connection"
  labels:
    {{- include "thyris-sz.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  restartPolicy: Never
  containers:
    - name: wget
      image: busybox:1.36
      command:
        - wget
      args:
        - -qO-
        - http://{{ include "thyris-sz.fullname" . }}:{{ .Values.service.port }}/healthz
